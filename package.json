{
  "name": "villa-catalog-cms",
  "version": "1.0.0",
  "description": "Villa Catalog Website with CMS using React.js, MongoDB, and Tailwind CSS",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["villa", "catalog", "cms", "react", "mongodb", "tailwind"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 